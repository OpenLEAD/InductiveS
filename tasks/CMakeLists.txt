# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(inductiveTaskLib)
ADD_LIBRARY(${INDUCTIVE_TASKLIB_NAME} SHARED 
    ${INDUCTIVE_TASKLIB_SOURCES})
add_dependencies(${INDUCTIVE_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${INDUCTIVE_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${INDUCTIVE_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${INDUCTIVE_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${INDUCTIVE_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${INDUCTIVE_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${INDUCTIVE_TASKLIB_HEADERS}
    DESTINATION include/orocos/inductive)

